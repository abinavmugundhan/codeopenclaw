# Leave empty when the dashboard is served by the same Node app.
# If you deploy the dashboard separately, set this to your backend origin
# or API base, for example:
# VITE_API_BASE_URL=https://finance-agent.example.com
# VITE_API_BASE_URL=https://finance-agent.example.com/api
VITE_API_BASE_URL=
